# Core API runtime — pinned for reproducible builds (CI + reviewers get identical installs).
fastapi==0.141.1
starlette==1.7.0
uvicorn[standard]==0.54.0
pydantic==2.13.5
python-multipart==0.0.32
python-dotenv==1.2.2
sqlalchemy==2.0.31
pyyaml==6.0.1
httpx==0.28.1

# ML / inference. shap is a hard import (explainability.py). torch is an
# optional extra (requirements-ml.txt, guarded import in model.py).
# scapy is only imported when ENABLE_LIVE_CAPTURE=true.
scikit-learn==1.5.0
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2
shap==0.45.1
scapy==2.7.0

# Reporting.
fpdf2==2.7.9

# Optional caching / shared state — degrades gracefully when Redis is absent.
redis==5.0.7
